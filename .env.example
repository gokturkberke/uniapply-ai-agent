# Copy this file to .env and adjust values for your environment.
# NEVER commit a real .env or any real API keys.

# --- Application ---
APP_NAME=UniApply AI Agent
ENVIRONMENT=development
API_VERSION=v1

# --- Registry & ingestion paths --------------------------------------------
# Curated source manifest (committed) and the local raw/normalized corpus dirs
# (gitignored). Defaults shown; override only if you relocate the data layout.
REGISTRY_PATH=data/registry/sources.json
RAW_DIR=data/raw
NORMALIZED_DIR=data/normalized
CHUNK_DIR=data/chunks
CHUNK_MAX_TOKENS=600
CHUNK_OVERLAP_TOKENS=80

# --- Future use (NOT wired up yet) -----------------------------------------
# These are placeholders for the upcoming RAG / LLM features. Leave commented
# until those features land. Set real values only in your local .env.
#
# LLM providers
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
#
# Vector store / embedding configuration will be added here when RAG lands,
# e.g. VECTOR_DB_URL=, EMBEDDING_MODEL=
