# Copy to .env.local (gitignored) to point the UI at a non-default backend.
# The backend dev server runs on http://localhost:8000 by default, so this is
# only needed when the API is hosted elsewhere.
VITE_API_BASE_URL=http://localhost:8000
