# Web framework & server
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0

# Validation & configuration
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
python-dotenv>=1.0,<2.0

# Ingestion / document normalization (raw PDF/HTML -> Markdown)
pymupdf4llm>=1.27,<2.0  # PDF -> Markdown (pulls PyMuPDF / fitz)
beautifulsoup4>=4.15,<5.0
markdownify>=1.2,<2.0

# Embeddings & vector index (multilingual, local-mode; no torch)
fastembed>=0.8,<0.9  # ONNX embeddings (multilingual MiniLM)
qdrant-client>=1.18,<2.0  # local/embedded mode (no Docker)

# LLM / grounded generation: local-first - mock (offline) + an OpenAI-compatible
# local server (Ollama / LM Studio) reached over httpx (listed under Testing). No paid SDK.

# Testing
pytest>=8.2,<9.0
httpx>=0.27,<1.0  # required by fastapi.testclient.TestClient
